---
import "../../../styles/concept-v6.css";
import "../../../styles/concept-v6-media.css";
import "../../../styles/cases.css";
import CasesLayout from "../../../layouts/CasesLayout.astro";
import CaseGrid from "../../../components/CaseGrid.astro";
import {
  caseStudies,
  getCase,
  getCategory,
  getRelatedCases,
  categoryPath,
} from "../../../data/cases";

export function getStaticPaths() {
  return caseStudies.map((c) => ({
    params: { category: c.category, slug: c.slug },
  }));
}

const { category, slug } = Astro.params;
const item = getCase(category!, slug!);
if (!item) {
  return Astro.redirect("/keysy/");
}

const cat = getCategory(item.category)!;
const related = getRelatedCases(item);
const hasStory = Boolean(
  item.painLead ||
    item.pains?.length ||
    item.plainSpeak?.length ||
    item.sections?.length ||
    item.journeyLead,
);
const serviceLine = [cat.title, ...item.services].join(" · ");
const painText = item.painLead || item.challenge;
const journeyText =
  item.journeyLead ||
  "Вот как мы провели клиента от боли до рабочего результата — без воды и «магии».";
const outcomeText =
  item.outcomeLead ||
  "В конце пути — измеримый результат и понятные следующие шаги.";

let chapter = 1;
const nPain = String(chapter++).padStart(2, "0");
const nJourney = String(chapter++).padStart(2, "0");
const nPlain = item.plainSpeak?.length ? String(chapter++).padStart(2, "0") : null;
const nSteps = item.sections?.length ? String(chapter++).padStart(2, "0") : null;
const nOutcome = String(chapter++).padStart(2, "0");
---

<CasesLayout
  title={`${item.title} — кейс ПРАЙМ`}
  description={`${item.metric} ${item.metricLabel}. ${item.summary}`}
  breadcrumb={[
    { label: "Главная", href: "/" },
    { label: "Кейсы", href: "/keysy/" },
    { label: cat.title, href: categoryPath(cat.id) },
    { label: item.client },
  ]}
>
  <article class="k-case k-case--story" style={`--k-accent:${cat.accent}`}>
    <header class="k-case-head k-case-head--visual k-reveal">
      <div class="k-case-head__copy">
        <p class="k-case-head__line">{serviceLine}</p>
        <h1>{item.title}</h1>
        <p class="k-case-head__lead">{item.summary}</p>
        <div class="k-case-head__bar">
          <p class="k-case-head__metric">
            <strong>{item.metric}</strong>
            <span>{item.metricLabel}</span>
          </p>
          <ul class="k-case-head__meta">
            <li>
              <span>Клиент</span>
              <strong>{item.client}</strong>
            </li>
            <li>
              <span>Отрасль</span>
              <strong>{item.industry}</strong>
            </li>
            <li>
              <span>Срок</span>
              <strong>{item.period}</strong>
            </li>
            {
              item.siteUrl && (
                <li>
                  <span>Сайт</span>
                  <strong>
                    <a href={item.siteUrl} target="_blank" rel="noopener">
                      {item.siteUrl.replace(/^https?:\/\//, "")}
                    </a>
                  </strong>
                </li>
              )
            }
          </ul>
        </div>
      </div>
      <figure class="k-case-head__visual">
        <button
          type="button"
          class="k-zoom k-case-head__frame"
          data-lightbox
          data-src={item.cover}
          data-alt={item.coverAlt}
          data-caption={item.coverAlt}
          aria-label={`Увеличить: ${item.coverAlt}`}
        >
          <span class="k-frame__chrome" aria-hidden="true">
            <i></i><i></i><i></i>
          </span>
          <span class="k-case-head__shot">
            <img src={item.cover} alt={item.coverAlt} />
          </span>
        </button>
        <figcaption>{item.siteUrl ? "Актуальный вид" : item.coverAlt}</figcaption>
      </figure>
    </header>

    {
      item.intro && (
        <section class="k-story-band k-reveal">
          <p class="k-story-kicker">О клиенте</p>
          <p class="k-story-lead">{item.intro}</p>
        </section>
      )
    }

    {/* 01 — Боль */}
    <section class="k-story k-reveal" aria-labelledby="story-pain">
      <div class="k-story__head">
        <span class="k-story__n">{nPain}</span>
        <div>
          <h2 id="story-pain">С какой болью пришли</h2>
          <p>{painText}</p>
        </div>
      </div>

      <div class:list={["k-split", !item.before && !item.cover && "k-split--solo"]}>
        <div class="k-split__copy">
          {
            item.pains && item.pains.length > 0 ? (
              <ol class="k-pain-list">
                {item.pains.map((p) => (
                  <li>
                    <h3>{p.title}</h3>
                    <p>{p.text}</p>
                  </li>
                ))}
              </ol>
            ) : (
              <p class="k-story-body">{item.challenge}</p>
            )
          }
        </div>
        {
          (item.before || item.cover) && (
            <figure class="k-split__media">
              <button
                type="button"
                class="k-zoom k-frame"
                data-lightbox
                data-src={item.before?.src ?? item.cover}
                data-alt={item.before?.alt ?? item.coverAlt}
                data-caption={item.before?.caption ?? item.coverAlt}
                aria-label={`Увеличить: ${item.before?.alt ?? item.coverAlt}`}
              >
                <span class="k-frame__chrome" aria-hidden="true">
                  <i></i><i></i><i></i>
                </span>
                <img
                  src={item.before?.src ?? item.cover}
                  alt={item.before?.alt ?? item.coverAlt}
                />
              </button>
              <figcaption>{item.before?.caption ?? "Точка старта"}</figcaption>
            </figure>
          )
        }
      </div>
    </section>

    {/* 02 — Путь */}
    <section class="k-story k-reveal" aria-labelledby="story-journey">
      <div class="k-story__head">
        <span class="k-story__n">{nJourney}</span>
        <div>
          <h2 id="story-journey">Как мы провели</h2>
          <p>{journeyText}</p>
        </div>
      </div>
      <ol class="k-journey">
        {
          item.approach.map((step, i) => (
            <li>
              <span class="k-journey__n">{String(i + 1).padStart(2, "0")}</span>
              <p>{step}</p>
            </li>
          ))
        }
      </ol>
    </section>

    {/* 03 — Простыми словами */}
    {
      item.plainSpeak && item.plainSpeak.length > 0 && (
        <section class="k-story k-reveal" aria-labelledby="story-plain">
          <div class="k-story__head">
            <span class="k-story__n">{nPlain}</span>
            <div>
              <h2 id="story-plain">Простыми словами</h2>
              <p>
                Сложные штуки из брифа — без жаргона. Так мы объясняли клиенту, зачем каждый шаг.
              </p>
            </div>
          </div>
          <div class="k-plain-grid">
            {item.plainSpeak.map((row) => (
              <article class="k-plain">
                <div class="k-plain__side k-plain__side--tech">
                  <span class="k-plain__label">Как говорят специалисты</span>
                  <p>{row.jargon}</p>
                </div>
                <div class="k-plain__side k-plain__side--human">
                  <span class="k-plain__label">Как мы объясняли</span>
                  <p>{row.plain}</p>
                </div>
              </article>
            ))}
          </div>
        </section>
      )
    }

    {/* 04 — Шаги с экранами */}
    {
      item.sections && item.sections.length > 0 ? (
        <section class="k-story k-reveal" aria-labelledby="story-steps">
          <div class="k-story__head">
            <span class="k-story__n">{nSteps}</span>
            <div>
              <h2 id="story-steps">Что сделали по шагам</h2>
              <p>Каждый шаг — конкретное изменение. Слева смысл, справа экран.</p>
            </div>
          </div>
          <div class="k-chapters">
            {item.sections.map((s, i) => (
              <article class:list={["k-chapter", i % 2 === 1 && "k-chapter--flip"]}>
                <div class="k-chapter__copy">
                  <span class="k-chapter__n">{String(i + 1).padStart(2, "0")}</span>
                  <h3>{s.title}</h3>
                  <p>{s.body}</p>
                </div>
                {s.image && (
                  <figure
                    class:list={[
                      "k-chapter__media",
                      s.fit === "contain" && "k-chapter__media--contain",
                    ]}
                  >
                    <button
                      type="button"
                      class="k-zoom k-frame"
                      data-lightbox
                      data-src={s.image}
                      data-alt={s.title}
                      data-caption={s.title}
                      aria-label={`Увеличить: ${s.title}`}
                    >
                      <span class="k-frame__chrome" aria-hidden="true">
                        <i></i><i></i><i></i>
                      </span>
                      <img src={s.image} alt={s.title} loading="lazy" />
                    </button>
                  </figure>
                )}
              </article>
            ))}
          </div>
        </section>
      ) : (
        !hasStory &&
        item.cover && (
          <figure class="k-case-cover k-reveal">
            <button
              type="button"
              class="k-zoom"
              data-lightbox
              data-src={item.cover}
              data-alt={item.coverAlt}
              data-caption={item.coverAlt}
              aria-label={`Увеличить: ${item.coverAlt}`}
            >
              <img src={item.cover} alt={item.coverAlt} />
            </button>
            <figcaption>{item.coverAlt}</figcaption>
          </figure>
        )
      )
    }

    {/* Доказательства — коротко, не свалка */}
    {
      item.gallery && item.gallery.length > 0 && (
        <section class="k-story k-story--evidence k-reveal" aria-labelledby="story-proof">
          <div class="k-story__head">
            <span class="k-story__n">·</span>
            <div>
              <h2 id="story-proof">Ещё кадры с проекта</h2>
              <p>Не галерея ради галереи — пара экранов, которые закрывают картину.</p>
            </div>
          </div>
          <div class="k-proof">
            {item.gallery.slice(0, 4).map((g) => (
              <figure class:list={["k-proof__item", g.fit === "contain" && "k-proof__item--contain"]}>
                <button
                  type="button"
                  class="k-zoom k-frame"
                  data-lightbox
                  data-src={g.src}
                  data-alt={g.alt}
                  data-caption={g.caption ?? g.alt}
                  aria-label={`Открыть: ${g.alt}`}
                >
                  <span class="k-frame__chrome" aria-hidden="true">
                    <i></i><i></i><i></i>
                  </span>
                  <img src={g.src} alt={g.alt} loading="lazy" />
                </button>
                {(g.caption || g.alt) && <figcaption>{g.caption ?? g.alt}</figcaption>}
              </figure>
            ))}
          </div>
        </section>
      )
    }

    {/* Итог */}
    <section class="k-story k-story--outcome k-reveal" aria-labelledby="story-outcome">
      <div class="k-story__head">
        <span class="k-story__n">{nOutcome}</span>
        <div>
          <h2 id="story-outcome">К чему пришли</h2>
          <p>{outcomeText}</p>
        </div>
      </div>

      <div class="k-outcome">
        {
          item.highlights && item.highlights.length > 0 && (
            <div>
              <h3>Что получили</h3>
              <ul class="k-case-checks">
                {item.highlights.map((h) => (
                  <li>{h}</li>
                ))}
              </ul>
            </div>
          )
        }
        <div>
          <h3>В цифрах</h3>
          <div class="k-case-kpi">
            {item.results.map((r) => (
              <div>
                <strong>{r.value}</strong>
                <span>{r.label}</span>
              </div>
            ))}
          </div>
          {
            item.quote && (
              <blockquote class="k-case-quote">
                <p>«{item.quote.text}»</p>
                <footer>
                  <strong>{item.quote.author}</strong> — {item.quote.role}
                </footer>
              </blockquote>
            )
          }
        </div>
      </div>
    </section>

    <section class="k-case-cta k-reveal">
      <h2>{item.ctaTitle ?? "Нужен такой же результат?"}</h2>
      <p>
        {item.ctaText ??
          "С 2015 на рынке: за 10+ лет немало агентств закрылось — мы на месте. Разберём нишу за 30 минут и подскажем формат: SEO, Директ или сайт."}
      </p>
      <div class="k-case-cta__actions">
        <a class="k-btn k-btn--primary" href="/kontakty/">Оставить заявку</a>
        <a class="k-btn k-btn--ghost" href={categoryPath(cat.id)}>Ещё {cat.short}</a>
      </div>
    </section>

    {
      related.length > 0 && (
        <section class="k-case-block k-reveal">
          <header class="k-case-block__head">
            <h2>Другие проекты</h2>
            <p>Похожие кейсы и соседние направления</p>
          </header>
          <CaseGrid cases={related} />
        </section>
      )
    }
  </article>
</CasesLayout>
